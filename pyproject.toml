[project]
name = "my_project"
version = "0.0.1"
dependencies = []

[project.optional-dependencies]
dev = ["pykwalify"]
test = ["pytest"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
